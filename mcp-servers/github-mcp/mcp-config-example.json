{
  "mcpServers": {
    "github": {
      "command": "node",
      "args": ["${HOME}/apps/careerAI/mcp-servers/github-mcp/dist/index.js"],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}